# تقرير شامل: نظام الذكاء الاصطناعي والتجويد والتكرار المتباعد

## 1. استخدام الذكاء الاصطناعي لتوليد الاختبارات
يعتمد المشروع على نموذج **Gemini 3 Flash Preview** لتوليد الأسئلة بشكل ديناميكي، وذلك من خلال صنفين أساسيين:

### اختبارات التحدي والاختبارات الشاملة (للسور) - `McqQuizService`
*   تقوم هذه الخدمة باختيار 8 آيات عشوائية من السورة المختارة وإرسالها إلى واجهة برمجة تطبيقات الذكاء الاصطناعي (API) كـ "سياق".
*   يتم توجيه الذكاء الاصطناعي لتوليد **5 أسئلة متدرجة الصعوبة** (سهل، متوسط، صعب، تحدي).
*   **أنواع الأسئلة المعتمدة:**
    *   إكمال الكلمة الناقصة.
    *   ما هي الآية التالية.
    *   معاني المفردات.
    *   خواتيم الآيات.
    *   المتشابهات اللفظية (مع سور أخرى).
    *   الآية السابقة.
*   يُجبر النموذج على الرد بصيغة `JSON` مهيكلة تتضمن كافة تفاصيل السؤال والخيارات الأربعة والإجابة الصحيحة.

### اختبار "أكمل الآية" - `QuizGeneratorService`
*   تُستخدم لتوليد سؤال مخصص لآية معينة.
*   تُرسل الآية المحددة للذكاء الاصطناعي ليقوم بصياغة سؤال حول موضع الآية، أو الآية التي تليها، أو الكلمات ضمنها.

### نظام الطوارئ (Fallback)
في حال تعطل خدمة الذكاء الاصطناعي أو عدم وجود مفتاح API، يعمل دالة `generateLocal` على توليد أسئلة برمجياً من قاعدة البيانات (عن طريق إخفاء كلمات عشوائية ووضع كلمات من آيات أخرى كخيارات خاطئة).

---

## 2. حل مشكلة اختفاء زر "إظهار التجويد"
الزر في واجهة التطبيق (`show.blade.php`) مرتبط بالشرط التالي:
`@if($hasTajweed)`
والذي يتحقق من وجود نصوص ملونة بالتجويد (`text_tajweed`) ضمن جداول الآيات.
عندما قام زميلك بنسخ المشروع من GitHub، كانت قاعدة البيانات لديه فارغة من هذه النصوص (التي تجلب عبر API خارجي).

**الحل:**
يجب عليه تشغيل أمر الجلب الخاص بالتجويد في سطر الأوامر (Terminal) لتعبئة قاعدة البيانات:
```bash
php artisan quran:sync-tajweed
```
*(ملاحظة: إذا كانت قاعدة البيانات لديه فارغة تماماً من القرآن، يجب عليه أولاً تشغيل `php artisan quran:sync`)*

---

## 3. خوارزمية التكرار المتباعد (Spaced Repetition)
في هذا المشروع، **لا توجد تعليمة (Command) منفصلة** لتشغيل خوارزمية التكرار المتباعد.
الخوارزمية مدمجة برمجياً داخل خدمة `SpacedRepetitionService` وتعمل بشكل لحظي (تلقائي) عندما يقوم المستخدم بالآتي:
*   الانتهاء من تسميع مقطع (في `RecitationController` أو `HifzController`).
*   تقوم الخدمة بتحويل نسبة التشابه (دقة التسميع) إلى "مستوى جودة" (Quality Score) من 0 إلى 5.
*   يتم تحديث عامل السهولة (`easiness_factor`) وحساب عدد الأيام القادمة للمراجعة.
*   وأخيراً، يُحدث تاريخ المراجعة القادمة (`next_review_date`) في قاعدة البيانات.

---

## 4. الإشعارات والجدولة اللازمة (Scheduling & Notifications)
يعتمد النظام على ميزة "الجدولة" في Laravel لإرسال إشعارات وتنبيهات المراجعة للمستخدمين بناءً على تواريخ التكرار المتباعد التي تم حسابها.

تم تعريف الجدولة في ملف `routes/console.php` بالأمر التالي:
```php
Schedule::command('notifications:review-reminders')->hourlyAt(0);
```

**التعليمات اللازمة للتشغيل:**

1.  **لإرسال الإشعارات بشكل فوري (للتجربة):**
    ```bash
    php artisan notifications:review-reminders
    ```

2.  **لتفعيل الجدولة محلياً (أثناء التطوير):**
    اترك هذا الأمر يعمل في نافذة Terminal موجهة لمسار المشروع:
    ```bash
    php artisan schedule:work
    ```

3.  **على السيرفر الحقيقي (Production):**
    لجعل العملية آلية تماماً، يجب إضافة مهمة (Cron Job) واحدة في إعدادات السيرفر لتعمل كل دقيقة:
    ```bash
    * * * * * cd /path-to-your-project && php artisan schedule:run >> /dev/null 2>&1
    ```
